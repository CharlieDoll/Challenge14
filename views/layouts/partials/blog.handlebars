<section class="container">
  <h3 class="tech-blog">{{blog.name}}</h3>
  <section class="flex-row justify-center">
    {{#each blog.post as |post|}}
    {{> post-details}}
    {{/each}}
  </section>
</section>